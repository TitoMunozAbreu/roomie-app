spring:
  data:
    mongodb:
      username: roomie
      password: roomie
      host: localhost
      port: 27017
      database: household
      authentication-database: admin
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: notificationMessage:es.roomie.household.kafka.NotificationMessage

server:
  port: 8095